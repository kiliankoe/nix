{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests"
  ],
  "enabledManagers": [
    "custom.regex",
    "nix"
  ],
  "nix": {
    "enabled": true
  },
  "prHourlyLimit": 0,
  "minimumReleaseAge": "7 days",
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "hosts/kepler/services/docker/**/*.nix"
      ],
      "matchStrings": [
        "#\\s*renovate\\s+image\\s*=\\s*\"(?<depName>[^\"@:]+):(?<currentValue>[^\"@]+)(?:@(?<currentDigest>sha256:[0-9a-f]+))?\""
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Flake inputs: list on the dashboard only, PRs on demand via checkbox; manual `nix flake update` stays the primary flow",
      "matchManagers": ["nix"],
      "dependencyDashboardApproval": true,
      "minimumReleaseAge": "0 days"
    },
    {
      "description": "Keep immich server and machine-learning bumped together",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "ghcr.io/immich-app/immich-server",
        "ghcr.io/immich-app/immich-machine-learning"
      ],
      "groupName": "immich"
    },
    {
      "description": "Datastores: no automatic major bumps, those need a manual migration",
      "matchDatasources": ["docker"],
      "matchPackageNames": [
        "postgres",
        "clickhouse/clickhouse-server",
        "docker.io/valkey/valkey",
        "ghcr.io/immich-app/postgres"
      ],
      "matchUpdateTypes": ["major"],
      "enabled": false
    }
  ]
}
